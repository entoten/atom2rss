{
  "name": "arom2rss",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that converts GitHub Atom feeds to RSS 2.0 for the Microsoft Teams RSS app",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "wrangler": "^3.65.0"
  },
  "dependencies": {
    "fast-xml-parser": "^4.4.0"
  }
}
